# 기본 설정 (모든 파일의 공통 기준)

# 텍스트 파일은 자동으로 개행(EOL) 변환
* text=auto eol=lf

# UTF-8 인코딩 권장
*.java text working-tree-encoding=UTF-8
*.gradle text working-tree-encoding=UTF-8
*.kt text working-tree-encoding=UTF-8
*.xml text working-tree-encoding=UTF-8
*.yml text working-tree-encoding=UTF-8
*.yaml text working-tree-encoding=UTF-8
*.properties text working-tree-encoding=UTF-8
*.md text working-tree-encoding=UTF-8
*.txt text working-tree-encoding=UTF-8

# OS / IDE별 줄바꿈 처리
*.ps1 eol=crlf

# Windows에서 CRLF가 자동 변환되지 않도록
*.bat text eol=crlf

# Unix/Linux/맥 기반 스크립트는 LF 유지
*.sh text eol=lf

# 빌드 산출물 / 바이너리 파일은 그대로 저장
*.jar binary
*.war binary
*.ear binary
*.class binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary
*.exe binary

# Gradle Wrapper
gradlew text eol=lf
gradlew.bat text eol=crlf
gradle-wrapper.jar binary

# Git Diff / Merge 정책

# Java, YAML 등은 일반 텍스트 diff 사용
*.java diff
*.kt diff
*.xml diff
*.yml diff
*.yaml diff
*.gradle diff

# 문서 파일 (Markdown 등) 도 diff 가능
*.md diff

# 바이너리 파일은 diff/merge 비활성화
*.jar -diff -merge
*.png -diff -merge
*.jpg -diff -merge
*.jpeg -diff -merge
*.pdf -diff -merge
*.zip -diff -merge

# EOL 검사 무시 파일 (그대로 커밋)
*.patch -text
*.crt -text
*.pem -text

# Docker 설정 파일은 무조건 LF로 저장 (리눅스 호환성 필수)
Dockerfile text eol=lf
docker-compose.yml text eol=lf
